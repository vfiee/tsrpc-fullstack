{
  "name": "tsrpc-app",
  "version": "0.0.1",
  "private": true,
  "description": "tsrpc fullstack project",
  "scripts": {
    "dev": "run-p backend frontend",
    "backend": "cd backend && yarn dev",
    "frontend": "cd frontend && yarn dev",
    "test": "yarn test",
    "ia": "run-p install:backend install:frontend",
    "install:backend": "cd backend && yarn install",
    "install:frontend": "cd frontend && yarn install",
    "preinstall": "npx only-allow yarn"
  },
  "keywords": [
    "fullstack",
    "typescript",
    "tsrpc"
  ],
  "author": "vyron",
  "license": "MIT",
  "devDependencies": {
    "npm-run-all": "^4.1.5"
  },
  "dependencies": {
    "tsrpc": "^3.3.0"
  }
}
